<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Osaka Tokyo Weather</title>
    <link rel="icon" href="./icon.png" type="image/png">
    <style>
        body {
            margin: 0;
            padding: 0;
        }

        /* 背景：朝・昼 */
        .morning {
            background: linear-gradient(to bottom, #87CEEB, #FFFFFF);
            color: #0066ff;
        }

        /* 背景：夕 */
        .evening {
            background: linear-gradient(to bottom, #f17807f9, #ffffff);
            color: #0066ff;
        }

        /* 背景：夜 */
        .night {
            background: linear-gradient(to bottom, #071161, #9598b1);
            color: #FFFF;
        }

        /* コンテナ */
        .container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        /* グリッドレイアウト */
        .grid-container {
            display: grid;
            grid-template-columns: auto auto;
            grid-template-rows: auto auto auto;
            padding: 10px;
            text-align: center;
        }

        /* タイトル */
        .title {
            grid-column: 1/3;
            grid-row: 1;
        }

        /* 更新時間 */
        .time {
            grid-column: 1/3;
            grid-row: 3;
        }
    </style>
</head>

<body>
    <!-- コンテナ -->
    <div class="container <%= timeClass %>">
        <!-- グリッドレイアウト -->
        <div class="grid-container">
            <!-- タイトル -->
            <div class="title">
                <h1>Osaka Tokyo Weather</h1>
            </div>
            <!-- 大阪 -->
            <div class="osaka">
                <h2>大阪</h2>
                <p>
                    <%= osaka["title"] %>
                </p>
                <img src='<%= osaka["url"] %>' alt="Weather Image">
            </div>
            <!-- 東京 -->
            <div class="tokyo">
                <h2>東京</h2>
                <p>
                    <%= tokyo["title"] %>
                </p>
                <img src='<%= osaka["url"] %>' alt="Weather Image">
            </div>
            <!-- 更新時間 -->
            <div class="time">
                <p>最終更新時刻： <%= new Date().toLocaleString("ja-JP", { timeZone: "Asia/Tokyo" }) %>
                </p>
            </div>
        </div>
</body>

</html>